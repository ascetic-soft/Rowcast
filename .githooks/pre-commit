#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

echo "Running PHP CS Fixer..."
./vendor/bin/php-cs-fixer fix

# Stage formatter updates for already tracked files.
git add -u

echo "Running PHPStan..."
./vendor/bin/phpstan analyse
